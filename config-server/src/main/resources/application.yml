server:
  port: ${PORT:8888}
management:
  security:
    enabled: false
spring:
  cloud:
    config:
      server:
        git:
          uri: ${CONFIG_SERVER_GIT_URI:https://github.com/utsman/config-repo}